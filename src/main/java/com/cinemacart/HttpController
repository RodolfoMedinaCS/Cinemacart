package com.cinemacart;

import com.sun.net.httpserver.HttpHandler;
import com.sun.net.httpserver.HttpExchange;
import java.io.IOException;
import java.io.InputStream;
import java.io.OutputStream;
import java.util.Scanner;
import com.google.gson.Gson;

/**
* Prototype HTTP handler class, to be called within the various Controller class files to simplify in an effort to refactor a large amount of code
* UNTESTED
**/


public class HttpRequestController implements HttpHandler {

  public void processHttpRequest(HttpExchange exchange) throws IOException {

          String response = "";
          int status;

          exchange.getResponseHeaders().add("Access-Control-Allow-Origin", "*");
          exchange.getResponseHeaders().add("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
          exchange.getResponseHeaders().add("Access-Control-Allow-Headers", "Content-Type");
          if (exchange.getRequestMethod().equalsIgnoreCase("OPTIONS")) {
              exchange.sendResponseHeaders(204, -1);
              return;
          }

          InputStream inputStream = exchange.getRequestBody();
          Scanner scanner = new Scanner(inputStream).useDelimiter("\\A");
          String requestBody;

          if (scanner.hasNext()) {
              requestBody = scanner.next();
          } else {
              requestBody = "";
          }
          scanner.close();

          Gson gson = new Gson();
}

public class sendHttpResponse(HttpExchange exchange) {
  exchange.sendResponseHeaders(status, response.getBytes().length);
  OutputStream os = exchange.getResponseBody();
  os.write(response.getBytes());
  os.close();
}
}
